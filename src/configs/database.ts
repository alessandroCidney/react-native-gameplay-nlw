const DATABASE_NAME = '@gameplay';

// @nome-do-app:nome-da-coleção

const COLLECION_USERS = `${DATABASE_NAME}:users`;
const COLLECION_APPOINTMENTS = `${DATABASE_NAME}:appointments`;

export {
	COLLECION_USERS,
	COLLECION_APPOINTMENTS
}
